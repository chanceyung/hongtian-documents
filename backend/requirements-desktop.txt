# ============================================
# 弘天文档 桌面版 — Python 依赖
# ============================================
# 此文件用于 PyInstaller 打包，仅包含客户端必要依赖。
# 排除了 Redis、Docling、Playwright 等重量级/服务端依赖。

# ---- Web 框架 ----
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ---- 数据库（SQLite 替代 Redis）----
aiosqlite>=0.20.0

# ---- LLM & 工作流 ----
openai>=1.12.0
instructor>=1.2.0

# ---- 文档解析 ----
python-pptx>=0.6.23
python-docx>=1.1.0
openpyxl>=3.1.2
PyMuPDF>=1.23.26
markdown-it-py>=3.0.0

# ---- PDF 渲染（轻量方案）----
PyPDF2>=3.0.1
weasyprint>=61.0

# ---- 图片处理 ----
Pillow>=10.2.0

# ---- HTML 解析 ----
beautifulsoup4>=4.12.3
lxml>=5.1.0
html5lib>=1.1

# ---- HTTP 客户端 ----
httpx>=0.27.0

# ---- 加密 ----
cryptography>=42.0.0

# ---- 工具 ----
tenacity>=8.2.3
structlog>=24.0.0